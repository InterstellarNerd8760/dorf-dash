/**
 * Source of truth is the live Grok Build app.
 * Discord webhook is server-only there and is never committed here.
 * Static GitHub Pages copy: ../index.html + ../app.js (copy-to-clipboard only).
 */
export {};
